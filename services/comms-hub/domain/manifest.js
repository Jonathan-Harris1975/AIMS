// Legacy import path retained for compatibility. The canonical migration list
// lives in services/comms-hub/migrations/manifest.js so the two paths cannot drift.
export { COMMS_HUB_REQUIRED_MIGRATIONS } from "../migrations/manifest.js";
export { default } from "../migrations/manifest.js";
