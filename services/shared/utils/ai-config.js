// Compatibility entrypoint: keep the historical shared-utils import path while
// maintaining a single source of truth for provider and route configuration.
export { aiConfig } from "../../../ai-config.js";
export { default } from "../../../ai-config.js";
