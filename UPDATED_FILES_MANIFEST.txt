AIMS Koyeb build-stage fix - updated files only

Root cause from latest log:
- npm ci was still trying to download packages from an internal OpenAI/caas Artifactory URL embedded in package-lock.json.
- Koyeb/GitHub cannot reach packages.applied-caas-gateway1.internal.api.openai.org, causing ETIMEDOUT during dependency installation.

Files in this patch:
- .npmrc
- .github/workflows/ci.yml
- Dockerfile
- nixpacks.toml
- package.json
- package-lock.json
- services/blotato/utils/autoPublishService.js

Key fixes:
- All internal Artifactory tarball URLs removed from package-lock.json.
- Installs pinned to https://registry.npmjs.org/.
- Node 22 used for CI, Docker, and Nixpacks.
- Docker/Nixpacks explicitly set registry before npm ci.
- supertest restored to devDependencies; production Koyeb installs do not need test-only packages.
- Blotato publish job result now includes source.article for existing status consumers/tests.
- Confirmed no SHIPPER/SHIPER references in repo or env workbook strings.

Validated locally:
- npm ci --omit=dev --no-audit --no-fund --ignore-scripts
- npm run build
- node scripts/startupCheck.js with minimal production env
- npm ci --no-audit --no-fund --ignore-scripts
- npm test: 38 passed, 0 failed
- node --check across JS files
